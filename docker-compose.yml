
version: '2'

services:
  angular-pwa-seed:
    image: angular-pwa-seed
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 80:80
      - 443:443